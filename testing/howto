per installare tutto basta fare
> npm install
oppure
> npm i


creare un file con estensione .test.js
lanciare "npm test"

Per testare le request JEST dovete startare il server e poi fare gli npm test, per le super non serve

I file chiamateli tipo jestTH.test.js, jestMA.test.js, jestMI.test.js, jestGI.test.js e uguale per i super cosi non si creano conflitti tnx
